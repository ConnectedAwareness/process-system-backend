{
  "name": "@connectedawareness/process-system-interfaces",
  "version": "0.0.4",
  "description": "lib of interfaces for process system",
  "repository": {
    "type": "git",
    "url": "https://github.com/ConnectedAwareness/process-system-backend.git"
  },
  "main": "dist/index.js",
  "source": "src/index.ts",
  "scripts": {
    "build": "npm run pre-build && tsc && npm run build-doc",
    "build-doc": "typedoc --module commonjs --out ./doc ./src --theme minimal",
    "pre-build": "rimraf dist && rimraf doc"
  },
  "keywords": [
    "connected awareness", "process system"
  ],
  "author": "jhberg",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.0.0",
    "rimraf": "^2.6.2",
    "ts-node": "^4.1.0",
    "tsconfig-paths": "^3.1.1",
    "tslint": "^5.7.0",
    "typedoc": "^0.11.1",
    "typescript": "^2.9.2"
  },
  "dependencies": {
    "tsc": "^1.20150623.0"
  },
  "rootDir": "src"
}
